name: Deploy to GitHub Pages

on:
  push:
    branches:
      - claude/game-of-life-interactive-mm9dpi
      - claude/particle-life-research-ldmjdw
      - main
  workflow_dispatch:

# Allow this workflow to publish to GitHub Pages.
permissions:
  contents: read
  pages: write
  id-token: write

# Only one concurrent deployment; cancel superseded runs.
concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npm run lint
      - run: npm run format:check
      - run: npm test
      - run: npm run build
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    # GitHub Pages only accepts deployments from the default branch, so on any
    # other branch this job would be rejected by the environment before it even
    # starts — a red run with no logs. Feature branches therefore stop after
    # build + test, which is the part that is actually worth checking there.
    if: github.ref_name == github.event.repository.default_branch || github.ref_name == 'main'
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
